{
  "name": "calculate",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first PWA calculator for GitHub Pages.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --check script.js && node calculator.test.js"
  },
  "engines": {
    "node": ">=24"
  }
}
